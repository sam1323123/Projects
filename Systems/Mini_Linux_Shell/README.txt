Project is a simple shell program using POSIX system calls.

The shell performs various user_entered commands from the terminal and also handles program execution.

The shell also utilises POSIX signals to handle to handle reaping and interrupts 
